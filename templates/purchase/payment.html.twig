{% extends "base.html.twig" %}

{% block title %}Payez votre commande avec Stripe
{% endblock %}



{% block body %}

	<h1>Payez votre commande avec Stripe</h1>

	<form id="payment-form">
  <div id="card-element" class="mt-5" >
    <!-- Elements will create input elements here -->
  </div>

  <!-- We'll put the error messages in this element -->
  <div id="card-errors" role="alert"></div>

  <button id="submit" class="btn btn-success mt-5">Payer</button>
</form>

{% endblock %}


{% block javascripts %}
	{{parent()}}
	<script src="https://js.stripe.com/v3/"></script>
   <script>
      const clientSecret = "{{clientSecret}}";
		const user = " {{ user }}";
      const stripePublicKey = '{{ stripePublicKey}}';
		const redirectAfterSuccessUrl = "{{ url('purchase_payment_success', {'id': purchase.id}) }}"
   </script>
	<script src="/js/payment.js"></script>
{% endblock %}